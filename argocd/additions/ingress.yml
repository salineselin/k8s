apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls.acme: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    cert-manager.io/cluster-issuer: nuggies-life-staging
spec:
  rules:
    - host: whatever.nuggies.life
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  name: https
  # tls:
  #   - hosts:
  #       - whatever.nuggies.life
  #     secretName: argocd-tls
# ---
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: argocd-nuggies-life
#   namespace: argocd
# spec:
#   secretName: argocd-secret
#   issuerRef:
#     # The issuer created previously
#     name: nuggies-life
#   dnsNames:
#     - argocd.nuggies.life
